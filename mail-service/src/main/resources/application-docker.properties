# Server Configuration
server.port=${SERVER_PORT:9102}

# RabbitMQ Docker Configuration
spring.rabbitmq.host=${SPRING_RABBITMQ_HOST:rabbitmq}
spring.rabbitmq.port=${SPRING_RABBITMQ_PORT:5672}
spring.rabbitmq.username=${SPRING_RABBITMQ_USERNAME:guest}
spring.rabbitmq.password=${SPRING_RABBITMQ_PASSWORD:guest}
spring.rabbitmq.connection-timeout=10000
spring.rabbitmq.template.retry.enabled=true
spring.rabbitmq.template.retry.initial-interval=1000
spring.rabbitmq.template.retry.max-attempts=3
spring.rabbitmq.template.retry.multiplier=1.0

# Application Specific Configuration
app.rabbitmq.exchange=${APP_RABBITMQ_EXCHANGE:user-exchange}
app.rabbitmq.queue=${APP_RABBITMQ_QUEUE:email-queue}
app.rabbitmq.routingkey=${APP_RABBITMQ_ROUTINGKEY:email.notification}
app.rabbitmq.listener.auto-startup=false

# SMTP (Mailtrap u otro SMTP)
# Leer directamente de variables MAIL_* del env_file para evitar sobrescrituras
spring.mail.host=${MAIL_HOST:sandbox.smtp.mailtrap.io}
spring.mail.port=${MAIL_PORT:2525}
spring.mail.username=${MAIL_USERNAME:7150a6ec81a0c1}
spring.mail.password=${MAIL_PASSWORD:958d190ad2d245}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.rabbitmq.listener.simple.missingQueuesFatal=false